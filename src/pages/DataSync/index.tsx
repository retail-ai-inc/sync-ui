// src/pages/DataSync/index.tsx
import React from 'react';
import DataSyncList from './DataSyncList';

const DataSyncIndex: React.FC = () => {
  // 如果需要加 TabLayout 或布局，可在此处理
  return <DataSyncList />;
};

export default DataSyncIndex;
